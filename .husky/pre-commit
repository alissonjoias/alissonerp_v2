#!/bin/sh
# ERP v2 — Pre-commit hook

echo "🔍 Verificando TypeScript..."
npx tsc --noEmit 2>&1 | tail -3
if [ $? -ne 0 ]; then
  echo "❌ Erros de TypeScript. Corrija antes de commitar."
  exit 1
fi

echo "✅ Pre-commit OK."
